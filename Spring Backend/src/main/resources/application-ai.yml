# Local Ollama — no cloud API keys. Swap chat-model if your machine can handle a larger one.
ollama:
  base-url: http://localhost:11434
  chat-model: llama3.2:3b
  embedding-model: nomic-embed-text
  connect-timeout: 5s
  generate-timeout: 60s
  embed-timeout: 15s
